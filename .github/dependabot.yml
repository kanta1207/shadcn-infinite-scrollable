version: 2
updates:
  - package-ecosystem: 'npm'
    directories:
      - '/'
    schedule:
      interval: 'weekly'
      day: 'monday'
    ignore:
      - dependency-name: '*'
        update-types: ['version-update:semver-major']
